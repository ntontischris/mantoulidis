// Root layout — minimal, no locale-specific logic here
// The [locale] layout handles the actual page rendering
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
